# Legacy theme regression fixtures

These frozen upstream samples exercise state transitions, mini mode, hit geometry, and SVG runtime behavior. They are test inputs only: they are not discovered as AgentHalo themes or included in application packages.

Runtime code still comes from `src/`. The authored sample manifests and assets remain unchanged so existing assertions continue to protect the same contracts.
